import { Button } from "./button"

const Generate = () => {
    return (
        <Button className="h-5 w-5 text-center" variant="default" size="sm">Generate</Button>
    )
}

export default Generate 